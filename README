Для тестирования input данные следует поместить в файл data.txt

Некорректные данные, поданные на вход обрабатываются следующим образом.
1. Если ошибку можно распознать однозначно, она игнорируется.
 - пустые строки игнорируются
 - если в строке не задана инструкция start, start считается равным 0
 - допускается отсутствие инструкций turn и\или walk
 - если после инструкции следует несколько числовых аргументов, все кроме первого игнорируются
2. Если ошибка является критической, выбрасывается исключение типа InvalidArgumentException
 - отсутствуют x,y координаты в начале строки
 - в титульной строки присутствует что-либо кроме одного числа